# _assets/private

General-purpose image asset stash for thumbnails and icons used across the site.

Drop image files here and reference them in `.url` files via:

```ini
Thumbnail=content/_assets/private/your-image.png
```

> Note: Files here are not wired into the app UI by default, but are publicly accessible by URL on the deployed site.
